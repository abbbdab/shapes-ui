---
title: Field
slug: field
description: Accessible form field components with built-in validation, labels, descriptions, and error messages.
---

<RenderPreview name="field-demo" />

## Installation

<InstallationBlock name="field" />

## Examples

### Default

Compose fields using `FieldRoot`, `FieldLabel`, `FieldControl`, and `FieldDescription`.

<RenderPreview name="field-demo" />

### With Description

Add a description to provide additional context for the field.

<RenderPreview name="field-description" />

### With Validation

Field components support validation with error messages that are automatically displayed when the field is invalid.

<RenderPreview name="field-validation" />

### Manual Composition

Use the individual Field subcomponents to create custom field layouts with full control over structure and styling.

<RenderPreview name="field-composition" />

### Custom Control

Replace the default `FieldControl` with any other input component for advanced use cases.

<RenderPreview name="field-custom-control" />
